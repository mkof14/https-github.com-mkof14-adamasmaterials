# Optional local overrides (not required for the public marketing site).
# Do not put secrets in client-side Vite defines.

# Public site URL (used for docs / previews only)
APP_URL=https://www.adamasmaterials.com
